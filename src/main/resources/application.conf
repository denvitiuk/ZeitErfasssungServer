
ktor {
  application {
    modules = [ com.yourcompany.zeiterfassung.ApplicationKt.module ]
  }
  deployment {
    port = 8080
  }

  # Пул подключений к Postgres через HikariCP
  database {
    url             = "jdbc:postgresql://localhost:5432/attendance"
    driver          = "org.postgresql.Driver"
    user            = "yuliyanatasheva"
    password        = "SOLGANn47@25"
    maximumPoolSize = 10
  }

  # Настройки JWT-аутентификации
  jwt {
    secret     = "HZfigEj52tw8ObMaGYbPZ+R2q0ds+qi0C584PfP7HDo="
    issuer     = "com.yourcompany.zeiterfassung"
    audience   = "zeiterfassung-client"
    realm      = "zeiterfassung"
    validityMs = 3600000
  }
}

# === Twilio-секреты ===
# Берём из .env или реальных ENV-переменных
twilio {
  accountSid = ${?TWILIO_SID}
  authToken  = ${?TWILIO_TOKEN}
  fromNumber = "+4917620363079"
}

# === Кэш (Caffeine) ===
# Если хотите вынести TTL в конфиг:
cache {
  verificationCode {
    expireMinutes = 5
  }
}