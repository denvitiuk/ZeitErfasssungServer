ktor {
  application {
    modules = [ com.yourcompany.zeiterfassung.ApplicationKt.module ]
  }
  deployment {
    port = 0                 # в тестах лучше “0” — выбирать свободный порт
  }
  jwt {
    secret     = "HZfigEj52tw8ObMaGYbPZ+qi0C584PfP7HDo="
    issuer     = "com.yourcompany.zeiterfassung"
    audience   = "zeiterfassung-client"
    realm      = "zeiterfassung"    # <- сюда смотрит ваш код
    validityMs = 3600000
  }
}

twilio {
  accountSid = ${?TWILIO_SID}
  authToken  = ${?TWILIO_TOKEN}
  fromNumber = ${?TWILIO_PHONE_NUMBER}
}